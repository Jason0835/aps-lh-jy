# 2026-07-24 工作日志

- 用户（Jason）查看当前记忆，确认三层记忆此前均为空（仅云端自动画像缓存文件存在但 `memoryBlock` 为空）。
- 用户要求建立记忆系统，初始化如下：
  - 工作区记忆目录 `.workbuddy/memory/`（新建）
  - 工作区长期笔记 `MEMORY.md`（新建）
  - 用户级记忆 `~/.workbuddy/MEMORY.md`（新建）
- 备注：项目规范要求所有输出使用简体中文，Java 8；修改排程须同步相关入口。
- 用户要求从记忆中去除"最小改动量原则"相关表述，已从工作区日志与用户级记忆中删除。
