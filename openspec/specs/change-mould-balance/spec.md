根据补充，核心要改成：**共用胎胚不是一律落到次数少的班，而是只有“落到早班且早班超过 8 次”时，才挪到中班；单胎胚永远不挪班。**

```text
请结合项目现有硫化排程代码，调整“换模/换活字块均衡与次数限制”逻辑，要求优先复用已有换模均衡、换模次数统计、换模时间计算、未排结果记录等逻辑，实在无法复用再做小范围重构。

一、核心规则

1. 每天换模/换活字块总次数严格限制为 15 次

规则说明：
- 每天最多允许 15 次换模/换活字块。
- 15 次包含共用胎胚和非共用胎胚的所有换模/换活字块次数。
- 如果当天超过 15 次，则当前换模/换活字块动作需要顺延到后一天。
- 如果最后一天 T+2 也超过 15 次，则进入未排结果，并输出明确日志。

2. 早班 8 次、中班 7 次不是硬性总控，只用于共用胎胚的换模均衡

规则说明：
- 严格硬控只有“每天总次数 15 次”。
- 早班 8 次、中班 7 次主要用于共用胎胚换模均衡。
- 早班/中班次数仍需统计，便于判断和日志排查。

3. 单胎胚，即非共用胎胚，不做班次挪动

规则说明：
- 如果 SKU 是单胎胚，不与本月其他待排物料共用胎胚代码，则不做早中班均衡。
- 单胎胚原本落在哪个班换模/换活字块，就在哪个班执行。
- 即使早班次数超过 8 次，单胎胚也不能挪到中班。
- 单胎胚仍然要累计到当天总换模次数 15 次限制中。
- 单胎胚也要累计到早班/中班次数统计中，但不参与均衡挪班。

4. 共用胎胚的均衡挪班规则

判断前提：
- 只有共用胎胚才允许触发早中班均衡。
- 是否共用胎胚的判断范围为：本月月计划中需要排产的物料。
- 如果当前物料的胎胚代码与其他本月待排物料胎胚代码相同，则认为是共用胎胚。
- 否则认为是单胎胚。

均衡规则：
- 共用胎胚如果原本落到中班，不需要因为中班次数超过 7 次再强制挪动，仍按项目现有逻辑处理。
- 共用胎胚如果原本落到早班，需要判断早班换模/换活字块次数是否超过 8 次。
- 如果早班次数未超过 8 次，则仍落在早班，不挪动。
- 如果早班次数超过 8 次，则需要挪到换模次数更少的中班。
- 挪到中班后，开产时间必须跟随中班换模/换活字块完成时间重新计算。
- 示例：共用胎胚原本落早班，早班已有 8 次，本次再落早班会超过 8 次，则本次挪到中班；如果是不共用胎胚，即使早班超过 8 次，也仍然在早班换模，不挪动。

5. 次数统计要求

每次确认最终换模/换活字块班次后，需要更新统计：
- 当天总换模/换活字块次数 +1。
- 最终落定班次对应的早班或中班次数 +1。
- 如果共用胎胚从早班挪到中班，则应统计到中班次数，不应再统计到早班。
- 如果单胎胚原本在早班，则统计到早班；原本在中班，则统计到中班。

二、参数要求

1. 换模均衡相关参数必须启用后才应用以上均衡逻辑。
2. 如果参数关闭，保持项目原有逻辑不变。
3. 每日总次数 15、早班均衡阈值 8、中班参考阈值 7，不要写死魔法值，优先从硫化参数或常量中读取。
4. 参数命名需符合项目现有硫化参数配置风格。

三、处理流程建议

1. 排程前初始化本月物料胎胚共用关系 Map，避免循环中频繁查库。
2. 换模/换活字块前，先判断当前 SKU 是否为共用胎胚。
3. 校验当天总换模/换活字块次数是否已达到 15 次：
   - 未达到：继续处理。
   - 已达到且非 T+2：顺延到后一天重新判断。
   - 已达到且为 T+2：写入未排结果。
4. 判断最终换模班次：
   - 单胎胚：保持原班次，不挪动。
   - 共用胎胚且原班次不是早班：保持原逻辑，不因中班超过 7 次强制挪动。
   - 共用胎胚且原班次是早班：
     - 如果早班次数未超过 8 次：仍在早班。
     - 如果早班次数超过 8 次：挪到中班。
5. 按最终班次重新计算换模/换活字块完成时间和开产时间。
6. 更新每日总次数、早班次数、中班次数。
7. 输出关键日志。

四、日志要求

需要输出以下关键日志：
- 当前 SKU、胎胚代码、是否共用胎胚。
- 原始换模班次。
- 当前日期已有总换模/换活字块次数。
- 早班已有次数、中班已有次数。
- 是否触发共用胎胚均衡挪班。
- 最终换模班次。
- 最终开产时间。
- 如果顺延，记录顺延原因和顺延日期。
- 如果进入未排，记录原因：T+2 换模/换活字块次数超过每日 15 次上限。

五、实现要求

1. 结合项目现有代码深入分析后再修改。
2. 优先复用已有换模均衡逻辑、换模次数统计逻辑、换模时间计算逻辑、未排结果逻辑。
3. 不要大范围重构，避免影响新增排产、续作排产、换活字块排产已有逻辑。
4. 需要同时覆盖换模和换活字块。
5. 注意性能，不要在循环中频繁查询数据库。
6. 本月物料胎胚共用关系建议一次性查询后缓存到排程上下文中。
7. 复杂判断必须添加详细中文注释。
8. 修改完成后补充关键场景测试或自测说明。

重点注意：
- 单胎胚永远不因为早班超过 8 次而挪到中班。
- 共用胎胚只有“原本落早班，且早班次数超过 8 次”时，才挪到中班。
- 每日 15 次是硬限制；早班 8 次、中班 7 次主要是均衡参考，不是总控硬限制。
```
