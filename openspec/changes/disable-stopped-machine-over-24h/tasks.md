## 1. 基础数据与上下文

- [ ] 1.1 梳理 `LhMachineOnlineInfo` 的可用时间字段与排程基准时间，明确“停机超过 24 小时”的计算口径并补充必要注释
- [ ] 1.2 在基础数据初始化链路中批量识别 MES 停机超过 24 小时的机台，并将统一禁排集合及日志明细挂载到 `LhScheduleContext`
- [ ] 1.3 为停机禁排机台输出过程日志，记录机台号、MES 最近在机时间、停机时长和禁排原因

## 2. 续作转新增主链复用

- [ ] 2.1 在 `ScheduleAdjustHandler` 续作/新增划分入口过滤停机禁排机台，避免受影响机台继续生成续作 SKU
- [ ] 2.2 让未命中有效续作机台的原续作 SKU 按现有 `newSpecSkuList` 主链进入新增待排队列，不新增独立排序分支
- [ ] 2.3 为“原续作 SKU 转入新增排产”输出过程日志，记录 SKU、原机台、转入原因及后续按新增规则排序

## 3. 统一选机过滤与验证

- [ ] 3.1 在 `DefaultMachineMatchStrategy` 接入统一禁排机台硬过滤，确保新增排产和续作补偿回流都不会再选中停机超时机台
- [ ] 3.2 补充定向回归，覆盖“机台禁排”“续作转新增后按原新增排序参与选机”“同物料多机台部分停机”场景
- [ ] 3.3 结合过程日志和候选机台过滤结果做一次定向验证，确认未引入额外兜底分支且原有新增约束仍生效
